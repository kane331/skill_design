# Robot 到 pytest-bdd 迁移 Skill 首次使用指南

本指南用于首次运行 [Robot 到 pytest-bdd 用例迁移 Skill](.github/skills/robot-pytest-bdd-migration/SKILL.md)。每次任务只迁移一个 `case_id`。

## 1. 启动位置与登录

在 `skill_design` 目录启动 GitHub Copilot CLI，使其能够发现 `.github/skills` 和 `.github/agents` 中的自定义内容。

确认已登录 Copilot CLI。首次访问旧项目、新项目或 Excel 所在的外部目录时，只授权本次迁移实际需要的明确路径；不要使用 `--allow-all` 或 `--yolo`。

## 2. 准备迁移配置

在你自己的工作区创建一份 JSON 配置。相对路径以该 JSON 文件所在目录为基准。

```json
{
  "legacy_robot_root": "/path/to/legacy-robot",
  "target_pytest_bdd_root": "/path/to/new-pytest-bdd",
  "test_case_excel": "/path/to/test-case.xlsx",
  "test_data_excel": "/path/to/test-data.xlsx",
  "artifacts_root": "/path/to/migration-artifacts",
  "cloud": {
    "trigger": {
      "executable": "company-cloud-cli",
      "args": ["trigger", "{case_id}", "{test_selector}"]
    },
    "poll": {
      "executable": "company-cloud-cli",
      "args": ["poll", "{case_id}"]
    }
  },
  "max_attempts": 3,
  "sensitive_field_patterns": ["自定义敏感字段名"]
}
```

配置只提供路径、命令和运行上下文，不是业务逻辑证据。不要将密码、Token、密钥或个人数据写入配置。

## 3. 检查路径与环境

- `legacy_robot_root`：旧 Robot Framework 项目，可读。
- `target_pytest_bdd_root`：新 pytest-bdd 项目，可读写；建议使用干净分支或独立 worktree。
- `test_case_excel`：包含参数配置与执行 flag Sheet 的旧测试用例 Excel。
- `test_data_excel`：包含多 Sheet、多行参数化运行数据的 Excel。
- `artifacts_root`：可写，并应加入实际项目的 `.gitignore`。
- `cloud.trigger` 和 `cloud.poll`：公司云端执行命令，必须使用 `executable` 加 `args`，不要拼接 shell 字符串。

云端结果应能提供执行状态、报告地址、实际测试标识和实际数据集标识。只有这些证据完整且测试通过，迁移才会标记为 `passed`。

## 4. 调用总控 Agent

在 Copilot CLI 中选择 `robot-migration-orchestrator`，然后输入：

```text
case_id=CASE-001；配置=/你的路径/migration.json
```

也可以从命令行直接指定总控 agent：

```shell
copilot --agent robot-migration-orchestrator --prompt "case_id=CASE-001；配置=/你的路径/migration.json"
```

## 5. 预期行为

- 总控先分析旧项目的 case ID 映射代码。映射不存在、多义或无法证实时立即停止，不会生成或修改迁移代码。
- `test case` Excel 中的执行 flag 不影响迁移、跳过标记、云端执行或统计。
- `test data` 的 Sheet、行、继承和组合规则必须来自旧项目数据读取代码的直接证据。
- 分析、追踪、审计、方案、实施、验证和审查报告均使用中文并写入 `artifacts_root`。
- 审计、静态修复、代码审查和云端代码归因修复最多各三轮；环境、设备、网络或证据不足的问题会停止并要求人工介入。
- 云端排队或不可用时，状态为“等待云端验证”；只有目标 case 的全部已证明数据集在云端通过才是 `passed`。

完整字段约定见 [配置说明](.github/skills/robot-pytest-bdd-migration/references/configuration.md)，流程规则见 [阶段契约](.github/skills/robot-pytest-bdd-migration/references/stage-contract.md)。